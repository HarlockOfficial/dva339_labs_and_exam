// pass

int main(){
	int a;
	int b;
	int c;

	a = 1;
	b = 2;
	c = 3;

	print(a, b, c);

	a = a * (a + c) / 2 - 4;
	b = b*2 - 1;
	c = a - b + c*(3-a);

	print(a, b, c);

	a = pow(a+1);
	b = b - pow(c);
	c = pow(a+b+2);

	print(a, b, c);

	return 1;
}

int pow(int x){
	return x*x;
}

int double(int x){
	return x/2;
}
