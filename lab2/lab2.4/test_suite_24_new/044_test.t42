// pass
int fib(int num) {
    if (num <= 1)
        return num;
    else
        return fib(num - 1) + fib(num - 2);
}
void main() {
    int i; i = 1;
    while (i <= 10) {
        print(fib(i));
        i = i + 1;
    }
}